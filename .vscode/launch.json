{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "tauri dev",
      "type": "node-terminal",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "command": ".\\scripts\\dev.cmd bun run tauri-dev",
      "env": {
        "BROWSERSLIST_ENV": "development"
      }
    }
  ]
}
